<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Expense Splitter - Tambah Pengeluaran Baru</title>
  </head>
  <body>
    <h1>Tambah Pengeluaran Baru untuk <%= room.name %></h1>
    <form action="/create/<%= roomId %>" method="POST">
      <div>
        <label for="name">Nama Pengeluaran:</label>
        <input type="text" id="name" name="name" required />
      </div>
      <div>
        <label for="paidBy">Dibayar Oleh:</label>
        <input type="text" id="paidBy" name="paidBy" required />
      </div>
      <div>
        <label for="total">Total Pengeluaran:</label>
        <input
          type="number"
          id="total"
          name="total"
          required
          min="0"
          step="0.01"
        />
      </div>
      <div>
        <label for="participants">Partisipan (pisahkan dengan koma):</label>
        <input
          type="text"
          id="participants"
          name="participants"
          required
          value="<%= room.participants.join(', ') %>"
        />
      </div>
      <button type="submit">Simpan Pengeluaran</button>
    </form>
    <a href="/dashboard/<%= roomId %>">Kembali ke Dashboard</a>
  </body>
</html>
