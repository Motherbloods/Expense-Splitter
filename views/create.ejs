<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Expense Splitter - Tambah Pengeluaran Baru</title>
    <link href="/styles/shared-styles.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script>
      window.roomId = "<%= room._id %>";
    </script>
    <script src="/js/create.js" defer></script>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"
    />
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1><i class="fas fa-plus-circle"></i> Tambah Pengeluaran Baru</h1>
        <div class="header-buttons">
          <a href="/dashboard/<%= room._id %>" class="btn btn-back">
            <span class="button-text">Kembali ke Dashboard</span>
            <i class="fas fa-arrow-left icon-mobile"></i>
          </a>
        </div>
      </div>
      <h2><%= room.name %></h2>
      <div id="errorMessage" style="color: red; display: none"></div>
      <form id="expenseForm" class="expense-form">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <div class="form-group">
          <label for="name"><i class="fas fa-tag"></i> Nama Pengeluaran:</label>
          <input type="text" id="name" name="name" required />
        </div>
        <div class="form-group">
          <label for="paidBy"><i class="fas fa-user"></i> Dibayar Oleh:</label>
          <input type="text" id="paidBy" name="paidBy" required />
        </div>
        <div class="form-group">
          <label for="total"
            ><i class="fas fa-money-bill-wave"></i> Total Pengeluaran:</label
          >
          <input
            type="number"
            id="total"
            name="total"
            required
            min="0"
            step="0.01"
          />
        </div>
        <div class="form-group">
          <label for="participants"
            ><i class="fas fa-users"></i> Partisipan (pisahkan dengan
            koma):</label
          >
          <input
            type="text"
            id="participants"
            name="participants"
            required
            value="<%= room.participants.join(', ') %>"
          />
        </div>
        <button type="submit" class="btn btn-add-expense">
          <i class="fas fa-save"></i> Simpan Pengeluaran
        </button>
      </form>
    </div>
  </body>
</html>
