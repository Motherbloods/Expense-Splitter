<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Expense Splitter - Tambah Pengeluaran Baru</title>
    <link href="/styles/shared-styles.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script>
      window.roomId = "<%= room._id %>";
    </script>
    <script src="/js/create.js" defer></script>
  </head>
  <body>
    <div class="container">
      <h1>Tambah Pengeluaran Baru</h1>
      <h2><%= room.name %></h2>
      <div id="errorMessage" style="color: red; display: none"></div>
      <form id="expenseForm">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <div>
          <label for="name">Nama Pengeluaran:</label>
          <input type="text" id="name" name="name" required />
        </div>
        <div>
          <label for="paidBy">Dibayar Oleh:</label>
          <input type="text" id="paidBy" name="paidBy" required />
        </div>
        <div>
          <label for="total">Total Pengeluaran:</label>
          <input
            type="number"
            id="total"
            name="total"
            required
            min="0"
            step="0.01"
          />
        </div>
        <div>
          <label for="participants">Partisipan (pisahkan dengan koma):</label>
          <input
            type="text"
            id="participants"
            name="participants"
            required
            value="<%= room.participants.join(', ') %>"
          />
        </div>
        <button type="submit" class="btn btn-add-expense" style="width: 100%">
          Simpan Pengeluaran
        </button>
      </form>
      <a href="/dashboard/<%= room._id %>" class="btn btn-back"
        >Kembali ke Dashboard</a
      >
    </div>
  </body>
</html>
