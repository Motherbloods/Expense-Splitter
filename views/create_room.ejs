<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Expense Splitter - Buat Room Baru</title>
    <link href="/styles/shared-styles.css" rel="stylesheet" />
    <script src="/js/create_room.js" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"
    />
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1><i class="fas fa-plus-square"></i> Buat Room Baru</h1>
        <div class="header-buttons">
          <a href="/list-rooms/<%= userId %>" class="btn btn-back">
            <span class="button-text">Kembali ke Daftar Room</span>
            <i class="fas fa-arrow-left icon-mobile"></i>
          </a>
        </div>
      </div>
      <div id="errorMessage" style="color: red; display: none"></div>
      <form id="createRoomForm" class="create-room-form">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <div class="form-group">
          <label for="name"><i class="fas fa-home"></i> Nama Room:</label>
          <input type="text" id="name" name="name" required />
        </div>
        <div class="form-group">
          <label for="createdBy"
            ><i class="fas fa-user-shield"></i> Admin:</label
          >
          <input type="text" id="createdBy" name="createdBy" required />
        </div>
        <div class="form-group">
          <label for="participants"
            ><i class="fas fa-users"></i> Partisipan (pisahkan dengan
            koma):</label
          >
          <input type="text" id="participants" name="participants" required />
          <small>Contoh: John, Jane, Bob</small>
        </div>
        <button type="submit" class="btn btn-create-room">
          <i class="fas fa-check-circle"></i> Buat Room
        </button>
      </form>
    </div>
  </body>
</html>
